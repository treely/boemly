import { IconProps } from 'phosphor-react';
import { COLORS } from './chakraCustomizations';

const iconCustomizations: IconProps = {
  color: COLORS.black,
  size: 16,
  weight: 'bold',
};

export default iconCustomizations;
